- id: cover_servo
  output: servo_output
  min_level: ${min_level}
  max_level: ${max_level}
  idle_level: ${idle_level}
  transition_length: ${transition_length}
  auto_detach_time: ${auto_detach_time}