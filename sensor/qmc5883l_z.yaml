- platform: qmc5883l
  field_strength_x:
    name: "${upper_devicename} Field Strength X"
    filters:
      throttle: ${x_throttle}
#   field_strength_y:
#     name: "${upper_devicename} Field Strength Y"
  field_strength_z:
    id: field_strength_z
    internal: true
    filters:
      delta: ${delta}
#    name: "${upper_devicename} Field Strength Z"
#   heading:
#     name: "${upper_devicename} Heading"
  range: 200uT
  oversampling: ${oversampling}
  update_interval: "${update_interval}"
